# Interior Backtick Fence Example

Opening prose paragraph describing a shell script snippet that quotes a backtick character
inside a comment, which is exactly the shape that used to make the old regex fail to pair.

```sh
# this comment quotes a literal ` backtick character on purpose
echo "hello world"
```

Closing prose paragraph that follows the fenced shell example, wrapping up the fixture's prose.
