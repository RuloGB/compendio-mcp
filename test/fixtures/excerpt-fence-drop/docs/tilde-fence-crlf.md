# Tilde Fence Example

Some opening prose paragraph explaining a hypothetical configuration example that a project
might declare in its own documentation, written in plain English sentences so this fixture
reads as ordinary corpus content rather than a synthetic test string.

~~~json
{
  "docsDir": ["docs", "adr"],
  "convention": {
    "mode": "loose"
  }
}
~~~

Some closing prose paragraph that follows the fenced example, explaining what a reader should
do next now that the configuration shape above has been shown to them in full.
